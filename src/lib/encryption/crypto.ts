export const encrypt = async (text: string, key: string) => {
  // Placeholder for encryption logic
  console.log('Encrypting:', text, 'with key:', key);
  return `Encrypted(${text})`;
};

export const decrypt = async (encryptedText: string, key: string) => {
  // Placeholder for decryption logic
  console.log('Decrypting:', encryptedText, 'with key:', key);
  return `Decrypted(${encryptedText})`;
};
